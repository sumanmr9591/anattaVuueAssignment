# anattaVuueAssignment
MEVN app 

Steps to run the app
# run "npm install" from base folder
# run "node server" from base folder
# Go to client folder and run "npm install"
# Go to client folder and run "npm run serve"
